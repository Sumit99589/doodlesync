{
  "name": "doodlesync",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client --production=false && npm run build --prefix client && npm install --prefix server",
    "start": "npm start --prefix server"
  }
}
